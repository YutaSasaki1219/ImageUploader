version: "3.7"
services:
  backend:
    build:
      context: ./backend
    container_name: api
    env_file: ./backend/.env
    ports:
      - "8000:8000"
    volumes:
        - ./backend/src:/app/src
